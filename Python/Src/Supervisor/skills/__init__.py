"""Built-in Skills the SupervisorAgent dispatches to.

Each module exposes a module-level ``SKILL`` instance that satisfies the
``Skill`` protocol. ``transformer_diagnosis`` wraps the entire Phase 3-5
LangGraph platform as one outer-level skill.
"""
