"""Examples of hot-pluggable Skills.

These are NOT auto-loaded — they exist so you can demo runtime
registration (``/load_dir examples/hot_skills`` from the CLI, or
``supervisor.register_skills_from_path(...)`` in code).
"""
