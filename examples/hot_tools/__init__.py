"""Examples of hot-pluggable Tools.

These are NOT auto-loaded — they exist so you can demo runtime
registration (``/load_dir examples/hot_tools`` from the CLI, or
``supervisor.register_tools_from_path(...)`` in code).
"""
